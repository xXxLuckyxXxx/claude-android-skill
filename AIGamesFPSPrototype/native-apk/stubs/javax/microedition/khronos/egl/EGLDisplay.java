package javax.microedition.khronos.egl;

/**
 * Compile-only stub (this android.jar omits the JSR-239 EGL package). The real
 * class is provided by the Android framework at runtime; this is compiled to a
 * separate classpath dir and deliberately NOT dexed.
 */
public interface EGLDisplay {
}
